# Migration `20200719235614-added-auth-provider-and-auth0-models-and-relations`

This migration has been generated by briansayles at 7/19/2020, 11:56:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."AuthProvider" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"id" text  NOT NULL ,
"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Auth0" (
"authProviderId" text  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"id" text  NOT NULL ,
"idToken" text   ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "AuthProvider_userId" ON "public"."AuthProvider"("userId")

CREATE UNIQUE INDEX "Auth0_authProviderId" ON "public"."Auth0"("authProviderId")

ALTER TABLE "public"."AuthProvider" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Auth0" ADD FOREIGN KEY ("authProviderId")REFERENCES "public"."AuthProvider"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200719231433-added-user-as-owner-of-player..20200719235614-added-auth-provider-and-auth0-models-and-relations
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -10,14 +10,31 @@
 model User {
   id   String @default(cuid()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @default(now())
+  authProvider AuthProvider
   name String
   credits Int @default(0)
   tournaments Tournament[]
   players Player[]
 }
+model AuthProvider {
+  id String @default(uuid()) @id
+  createdAt DateTime @default(now())
+  user User @relation(fields: [userId], references: [id])
+  userId String
+  auth0 Auth0
+}
+
+model Auth0 {
+  id String @default(uuid()) @id
+  createdAt DateTime @default(now())
+  authProvider AuthProvider @relation(fields: [authProviderId], references: [id])
+  authProviderId String
+  idToken String?
+}
+
 model Tournament {
   id String @default(uuid()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @default(now())
```


